import styled from "styled-components";

const Wrapper = styled.label`
  display: flex;
  width: 100%;
  align-items: center;
`;

const Label = styled.div`
  display: block;
  margin-right: 10px;
`;

const StyledInput = styled.input`
  display: block;
  width: 100%;
  font-size: 16px;
  padding: 0 10px;
`;

export { Wrapper, Label, StyledInput };
